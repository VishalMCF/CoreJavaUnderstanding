package demo.networking.networkscalibility.perconnection;

public class Client {
    public static void main(String[] args) {
        // same as before as in per rwquest package just don't close the socket before checking multiple requests
    }
}
